package com.veloso.crud.mappers;

public class ProdutoMapperTest {

	//@Test
	public void shouldMapProdutoToDto() {
		// given
//		Produto produto = new Produto(1L, "produto", 5, 5.0);

		// when
		//ProdutoDTO produtoDto = ProdutoMapper.INSTANCE.produtoToProdutoDTO(produto);

		// then
//		assertThat(produtoDto).isNotNull();
//		assertThat(produtoDto.getId()).isEqualTo(1L);
//		assertThat(produtoDto.getNome()).isEqualTo("produto");
//		assertThat(produtoDto.getEstoque()).isEqualTo(5);
//		assertThat(produtoDto.getPreco()).isEqualTo(5.0);
	}
	
//	@Test
	public void shouldMapDTOToProduto() {
		// given
//		ProdutoDTO produtoDto = new ProdutoDTO(1L, "produto", 5, 5.0);

		// when
//		Produto produto = ProdutoMapper.INSTANCE.produtoDTOToProduto(produtoDto);

		// then
//		assertThat(produto).isNotNull();
//		assertThat(produto.getId()).isEqualTo(1L);
//		assertThat(produto.getNome()).isEqualTo("produto");
//		assertThat(produto.getEstoque()).isEqualTo(5);
//		assertThat(produto.getPreco()).isEqualTo(5.0);
	}

}
